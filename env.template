# BrinkByte Vision - Frontend Environment Configuration
# Copy this to .env.local for local development

# tAPI Backend Server
VITE_TAPI_SERVER=localhost:8090

# Billing Server (for development, calls directly; in production, proxied through tAPI)
VITE_BILLING_SERVER=http://localhost:8081

# API Mode: 'development' or 'production'
# In development, billing calls go directly to billing server
# In production, all calls go through tAPI gateway
VITE_API_MODE=development

